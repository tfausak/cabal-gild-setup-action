{
  "jobs": {
    "action": {
      "runs-on": "${{ matrix.os }}-latest",
      "steps": [
        {
          "uses": "actions/checkout@v5"
        },
        {
          "uses": "./"
        },
        {
          "run": "cabal-gild --version"
        }
      ],
      "strategy": {
        "fail-fast": false,
        "matrix": {
          "os": [
            "macos",
            "ubuntu",
            "windows"
          ]
        }
      }
    }
  },
  "name": "Workflow",
  "on": {
    "push": null
  }
}
